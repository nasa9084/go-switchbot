name: test
on: [ push, pull_request ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Golang
        uses: actions/setup-go@v7
        with:
          go-version: 1.26

      - name: checkout
        uses: actions/checkout@v7

      - name: test
        run: go test -v
